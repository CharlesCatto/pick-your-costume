{
	"name": "pick-your-costume-server",
	"version": "1.0.0",
	"description": "Backend server for Pick Your Costume",
	"main": "dist/src/main.js",
	"type": "commonjs",
	"scripts": {
		"dev": "tsc --watch",
		"build": "tsc --project tsconfig.build.json",
		"start": "node dist/src/main.js",
		"migrate": "ts-node bin/migrate.ts",
		"seed": "ts-node bin/seed.ts",
		"reset-db": "npm run migrate && npm run seed",
		"check-db": "ts-node bin/check-db.ts",
		"render-build": "npm install && npm run build && npm run copy-files",
		"copy-files": "cp -r src/database dist/ && cp package.json dist/ && cp .env.production dist/ || true"
	},
	"keywords": [
		"express",
		"typescript",
		"mysql",
		"costume"
	],
	"author": "Charles Cattoir",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"dotenv": "^16.6.1",
		"express": "^4.19.2",
		"pg": "^8.16.3"
	},
	"devDependencies": {
		"@types/cors": "^2.8.19",
		"@types/express": "^4.17.23",
		"@types/node": "^20.19.13",
		"@types/pg": "^8.15.5",
		"nodemon": "^3.1.4",
		"ts-node": "^10.9.2",
		"typescript": "^5.4.5"
	}
}
